{
  "name": "dionscheduler-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Real backend: Stripe Connect (client payments to each business) + Stripe Billing (platform subscription revenue) + Supabase (database)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "stripe": "^16.8.0"
  }
}
